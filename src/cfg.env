# Переменные окружения должны иметь имя APP_ + имя переменной в верхнем регистре
# Параметр False надо передавать пустой строкой: ""

# Выводить дополнительную информацию
APP_VERBOSE="True"

# Использовать GPU
#APP_SAM2_FORCE_CUDA=""
APP_SAM2_FORCE_CUDA="True"


# ПАРАМЕТРЫ БАЗОВОГО АЛГОРИТМА
# Отбор масок по минимальному и максимальный размеру (по умолчанию True)
#APP_AUTO_CALCULATE_AREAS=""
#APP_AREA_MIN=1000
#APP_AREA_MAX=800000

# Расщепление точки центра масс: радиус, число точек, фильтровать ли цвет точек (по умолчанию APP_PROMPT_POINT_RADIUS=0 - не расщеплять) 
#APP_PROMPT_POINT_RADIUS=50
#APP_PROMPT_POINT_NUMBER=30
#APP_PROMPT_POINT_COLOR_FILTER="True"
#APP_PROMPT_POINT_COLOR_THRESH=20


# ПАРАМЕТРЫ АЛГОРИТМА ТАЙЛИНГА
# Размер перехлеста при тайлинге (по умолчанию 256)
#APP_TILING_OVERLAP=768
#APP_TILING_OVERLAP=512
#APP_TILING_OVERLAP=128
#APP_TILING_OVERLAP=64

# Инверсный режим тайлинга (промпт не элементов, а швов. По умолчанию False)
#APP_TILING_INVERSE_MODE="True"

# Сколько точек брать при подготовке точечных промптов в тайлах (по умолчанию 1000)
#APP_TILING_PROMPTS_NUM_POINTS=1000

# Расщепление точки центра масс на тайле (по аналогии с базовым алгоритмом)
#APP_TILING_PROMPT_POINT_RADIUS=100

# Постпроцессинг (постобработка маски - удаление шума, заливка мелких деталей. По умолчанию False, kernel (W,H) = (9,9)
#APP_TILING_POST_PROCESS="True"
#APP_TILING_POST_PROCESS_KERNEL_W=9
#APP_TILING_POST_PROCESS_KERNEL_H=9

